<!DOCTYPE html>
<html>
  <head>
    <title>Financial Trust</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Use Tailwind via CDN for instant styling without build step %>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <%# Minimal Turbo setup if importmaps are missing %>
    <script type="module">
      import hotwiredTurbo from 'https://cdn.skypack.dev/@hotwired/turbo';
    </script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-gray-100 text-gray-900">
    <nav class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <span class="font-bold text-xl text-blue-600">Financial Trust</span>
            </div>
          </div>
          <div class="flex items-center">
            <% if current_user %>
              <span class="text-gray-700 mr-4">Welcome, <%= current_user.name %></span>
              <% if current_user.admin? %>
                <%= link_to "Admin", admin_accounts_path, class: "text-blue-600 hover:text-blue-800 mr-4 font-semibold" %>
                <%= link_to "Transfer", new_admin_transfer_path, class: "text-blue-600 hover:text-blue-800 mr-4 font-semibold" %>
                <% pending_count = Deposit.where(status: 'pending').count %>
                <%= link_to admin_deposits_path, class: "relative text-gray-600 hover:text-gray-900 mr-4" do %>
                  Deposits
                  <% if pending_count > 0 %>
                    <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs font-bold px-1.5 py-0.5 rounded-full"><%= pending_count %></span>
                  <% end %>
                <% end %>
              <% else %>
                <%= link_to "Deposit Funds", new_deposit_path, class: "text-green-600 hover:text-green-800 mr-4 font-semibold" %>
              <% end %>
              <%= button_to "Logout", session_path, method: :delete, class: "text-gray-500 hover:text-gray-900 bg-transparent border-none cursor-pointer" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="min-h-screen p-6">
      <%= yield %>
    </div>
  </body>
</html>
